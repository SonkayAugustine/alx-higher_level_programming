#!/usr/bin/node
const firstLine = 'C is fun';
const secondLine = 'Python is cool';
const thirdLine = 'JavaScript is amazing';

function myFunc (a) {
  console.log(a);
}

myFunc(firstLine);
myFunc(secondLine);
myFunc(thirdLine);
